# External dependencies only
# Local dependencies (tta_aws, tta_f5, tta_types) are declared in pyproject.toml
# and installed separately in Docker/Makefile
runpod==1.7.9
pydub==0.25.1
requests==2.32.3